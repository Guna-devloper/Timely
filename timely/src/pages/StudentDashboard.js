import React from "react";
import FacultyDashboard from "./FacultyDashboard"; // Since students have the same view

function StudentDashboard() {
  return <FacultyDashboard />;
}

export default StudentDashboard;
